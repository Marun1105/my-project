fastapi
uvicorn[standard]
anthropic
sqlalchemy
psycopg2-binary
passlib[bcrypt]
bcrypt<4.1  # passlib 1.7.4's self-test breaks on bcrypt>=4.1 (unmaintained passlib, known incompatibility)
pyjwt[crypto]  # [crypto] pulls in `cryptography`; without it Google's RS256 raises NotImplementedError
httpx  # the Google token exchange; present transitively today, declared here on purpose
resend
twilio
pydantic[email]
python-dotenv
tzdata  # база с часови зони: Windows и слим Linux образи нямат системна такава
